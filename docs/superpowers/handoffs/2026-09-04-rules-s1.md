# Handoff: rules S1 (#330) — start execution in a fresh session

Written 2026-09-04 at the end of a long session. Everything below is
committed or filed; nothing lives only in chat.

## Where things are

- **Branch `feat/rules`** in worktree
  `~/work/vivace-graph-v3/.worktrees/rules-spec`, from `experiment`
  79bfe89 (the #322/#329 merge). Two commits, **not pushed** (Kevin
  batches pushes to limit CI runs): 4053d9a the #304 spec, 159728b the
  S1 plan. The main checkout `~/work/vivace-graph-v3` is shared with
  other sessions — never build or commit there.
- **Spec:** `docs/superpowers/specs/2026-09-04-rules-as-producers-design.md`
  (approved by Kevin 2026-09-04; §4 amended by the plan: namespaces are
  strings on the wire).
- **Plan:** `docs/superpowers/plans/2026-09-04-rules-s1-claims-as-facts.md`,
  four tasks, TDD, subagent-driven. Kevin had not yet answered the
  execution-approach question when the session ended; subagent-driven
  is the recommendation and his habit.
- **Issues:** #304 epic; sub-issues #330 S1 (this), #331 S2, #332 S3,
  #333 S4 (gated on #122). All on the SpatioTemporal Substrate board
  (project 1). Sitrep plan row for #304 recorded 2026-09-04 (week).
- **Engine worktree for downstream tests:**
  `~/work/vivace-graph-v3/.worktrees/experiment-merged`, detached at
  79bfe89; put it first on `asdf:*central-registry*` to test cl-llm
  against the merged engine.

## How to run

- Every suite in a subprocess from the worktree, worktree first on
  `asdf:*central-registry*`, target the `-test` system, read
  `Did N checks`; `asdf:test-system` on a non-test system is a silent
  no-op. Foreground only; a subagent that backgrounds a run stops and
  never returns.
- `graph-db/query-test` 33 / 0, `graph-db/gui-test` 635 / 0,
  `graph-db/spacetime-test` 650 / 0 are the current baselines.
- SDD: `scripts/sdd-workspace PLAN` under the superpowers skill gives
  the ledger dir; the ledger from #322's run was deleted with its
  worktree. Start fresh.

## Decisions that bind S1 (from the spec and the plan)

- `claim/7` generates from the subject, object or producer index by
  what is bound; nothing bound under a resource bound is refused as
  cost-unbounded (#285); nothing bound without one walks the family.
- Namespaces cross as strings, resolved with `find-symbol` in
  `KEYWORD`; unknown → no solutions, nothing interned. Unary claims
  bind the object pair to NIL. Standing returns as a lowercase string.
- Families are symbols the guard canonicalises (schema type names are
  admitted); from Lisp, the parent symbol.
- Retracted claims generate unless `claim-current/1` filters.
- `unknown-claim-family` must classify as ill-typed in
  `graph-db/query`'s `%ill-typed-condition-p` (by `find-symbol`, so
  `graph-db/query` stays independent of spacetime).

## What comes after S1

- S2 (#331): the stored `rule` record, `def-rule`, compile through the
  guard with recursion refused, `run-rule` sweep-then-insert with
  provenance, `extent-intersection` in cl-temporal-extent.
- Then blackboard slice 1 (kraison/blackboard#2): spec and plan exist
  there; two corrections are recorded on that issue (guarded type
  names are bare; columns are camelCase). Blackboard is private.
- cl-llm: nothing pending; #45 merged, engine floor is the #329 merge.

## Traps learned this week

- GitHub does not auto-close issues from merges into `experiment`;
  close them by hand with a pointer to the merge commit.
- The vivace-graph CI runner queues behind the post-merge `experiment`
  run; a PR's full suite takes ~40 minutes once it starts.
- Concurrent SBCL builds by other sessions can corrupt a shared FASL
  compile (a `cl+ssl` package-variance abort was one); rerun.
